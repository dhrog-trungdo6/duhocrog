# Crawler output files (chứa data crawl được)
urls.json
schools_data_scraped.json
schools_failed.json

# Scrape-test output (raw HTML + parsed JSON + report)
scrape-test/output/

# Batch crawl output (raw HTML cache gzip + summary)
batch-output/
